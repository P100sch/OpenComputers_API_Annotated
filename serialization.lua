---@class Serialization This module provides simple value serialization.
---@field serialize fun(value:any,pretty:boolean|number):string Generates a string from an object that can be parsed again using `serialization.unserialize`. The generated output is Lua code. Supports basic types (nil, boolean, number, string) and tables without cycles (will error out when cycles are detected, unless in `pretty` print mode). Properly handles NaN values and infinity. <br/>The `pretty` mode can be used to generate output for display to the user, this output will in most circumstances not be readable with `serialization.unserialize`. <br/>The value of pretty defines the number of entries that will be printed. <br/>If pretty is set to true it will by default print 10 entries. <br/>This function can be useful for sending messages via a network card.
---@field unserialize fun(value:string):any Restores an object previously saved with `serialization.serialize`.
local serialization = require("serialization")